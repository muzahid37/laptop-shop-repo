import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h2>Q:What is the difrents block elements and inline elements</h2>
            <p>Ans:HTML element has a default 2 display value .They are block and inline. Block-level element always starts on a new line, But An inline element does not start on a new line.n inline element only takes up as much width as necessary.</p>
            <h2>Q:What is Semantic tag</h2>
            <p>Ans:Semantic HTML that introduces meaning to the web page rather than just presentation. A semantic element clearly describes its meaning to both the browser and the developer.The core characteristic of a semantic element is that it clearly communicated its meaning to both the developer and the browser. These elements clearly define its content. The semantic HTML tags help the search engines and other user devices to determine the importance and context of web pages.The pages made with semantic elements are much easier to read.</p>
        </div>
    );
};

export default Blogs;